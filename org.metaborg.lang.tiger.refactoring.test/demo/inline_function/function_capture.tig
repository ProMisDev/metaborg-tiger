/* 
test file to inline multiple function calls
plus(1,1) -> 1 + 1
*/

let 
	var a := 1
	function plus(b:int) : int = (
		a + b
	)
in 
	let 
		var a:= 2
	in
   	plus(2);
   	plus(4)
  end
end
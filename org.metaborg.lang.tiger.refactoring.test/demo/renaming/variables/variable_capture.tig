/* 
test file to rename a variable inside a let binding to the same name as the varialbe of the outside let binding
y -> x
*/

let 
	var x := 10
	var r := 
	  let
		  var y := 100
	  in 
		  x + y
		end
  in
  	r
end
module renaming

imports
 nabl2/runtime
 pp

rules
	rename-menu-action :
    	terms@(selected-term, _, ast, path, project-path) -> (filename, result)
    where
    	(filename, result) := <rename-action(construct-textual-change|)> terms 